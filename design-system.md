ezzsearch 디자인 시스템
> 네이버 키워드 검색량 조회 도구를 시작으로 확장되는 ezzsearch 제품군 전반에 적용하는 디자인 기준 문서입니다. 색상·타이포그래피처럼 정해진 값은 그대로 쓰고, 레이아웃 패턴처럼 아직 열려 있는 선택지는 상황에 맞게 고르면 됩니다.

1. 브랜드 원칙
타겟 사용자: 소상공인, 엄마 등 — 마케팅/데이터 도구에 익숙하지 않을 수 있는 사용자
톤: 전문적이되 위압적이지 않게. 데이터는 정확하게, 표현은 쉽게.
로고 방향: 친근한 웜톤형 (코랄+앰버, 둥근 형태, 돋보기 아이콘)
원칙 3가지
숫자보다 의미를 먼저 — "1,200회" 앞에 "이 정도면 꽤 많이 찾는 키워드예요" 같은 해석을 붙인다
한 화면 = 한 가지 결정 — 정보를 한꺼번에 쏟아내지 않는다
전문 용어는 괄호로 풀어준다 — "검색량(한 달 동안 이 단어를 검색한 횟수)"

2. 로고 사용 가이드
버전	파일	사용처
기본형 (아이콘+워드마크)	`easyserch_logo.svg`	웹사이트 헤더, 이메일 서명, 문서 표지
아이콘 단독형	`easyserch_icon.svg`	파비콘, 앱 아이콘, 소셜 프로필, 좁은 공간
최소 여백: 로고 높이의 25%를 사방 여백으로 확보
금지 사항: 색상 임의 변경, 가로세로 비율 왜곡, 아이콘과 워드마크 재배치

3. 컬러
이름	역할	HEX
Coral	주 브랜드 색 — 버튼, 강조, 아이콘 배경	`#E06B3D`
Coral Dark	Coral의 hover/active 상태	`#C15429`
Amber	보조 강조 — 하이라이트, 배지	`#F5A83C`
Brand Text	Coral/Amber 배경 위에 올라가는 텍스트	`#7A3418`
Text Primary	본문 텍스트	`#3D2E1F`
Text Secondary	보조 설명, 캡션	`#8A7B6C`
Background	페이지 배경 (은은한 웜톤)	`#FFFBF7`
Surface	카드 배경	`#FFFFFF`
Border	구분선, 테두리	`#EDE6DD`
Success	긍정적 신호 (상승 추세 등)	`#4B8B5F`
Error	오류, 경고	`#C94F4F`
```css
:root {
  --color-primary: #E06B3D;
  --color-primary-hover: #C15429;
  --color-accent: #F5A83C;
  --color-text-on-brand: #7A3418;
  --color-text-primary: #3D2E1F;
  --color-text-secondary: #8A7B6C;
  --color-bg: #FFFBF7;
  --color-surface: #FFFFFF;
  --color-border: #EDE6DD;
  --color-success: #4B8B5F;
  --color-error: #C94F4F;
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 16px;
  --font-family: 'Pretendard', -apple-system, 'Noto Sans KR', sans-serif;
}
```
4. 타이포그래피
폰트: Pretendard (한글·영문 모두 커버, 국내 서비스에서 가장 무난하고 가독성 좋음) → 폴백으로 시스템 폰트, Noto Sans KR
용도	크기	굵기
H1 (페이지 제목)	28px	700
H2 (섹션 제목)	22px	600
H3 (카드 제목)	18px	600
본문	15px	400
보조/캡션	13px	400
버튼	15px	600
강조 숫자 (결과 수치)	36px	700
행간은 본문 1.6, 제목 1.3 기준.
5. 레이아웃 패턴 — 3가지 대안
지금 계획하신 "그래프 뷰 + CSV 다운로드" 구조를 어떤 틀에 담을지에 대한 선택지예요.
A. 데이터 대시보드형
좌측 사이드바 내비게이션 + 상단 요약 카드 4개 + 중앙 차트 + 하단 상세 테이블.
장점: 한 화면에서 정보량이 많고 전문 도구 느낌으로 신뢰감을 줌
단점: 처음 쓰는 소상공인에게는 정보가 많아 부담스러울 수 있음
적합: 키워드를 매일 반복 모니터링하는 파워유저
B. 질문-답변형 심플 UI
화면 중앙 큰 검색창 하나 → 결과는 큰 숫자 + 한 줄 해석 + 그래프 1개 → 상세 데이터는 "더보기"로 접어둠.
장점: 진입장벽이 낮고 브랜드의 코랄 톤과 잘 어울림, 모바일에서도 잘 작동
단점: 여러 키워드를 한번에 비교하긴 불편
적합: 가끔 궁금할 때 빠르게 확인하는 초보 사용자
C. 피드/카드 큐레이션형
대시보드 대신 "오늘의 트렌드" 카드가 쌓이는 피드. 관심 키워드를 등록해두면 변화가 있을 때 카드로 알림.
장점: 매번 검색할 필요 없이 자동으로 받아봄, 구독형 리포트 비즈니스 모델과 바로 연결됨
단점: 즉석에서 원하는 키워드를 바로 조회하는 용도로는 약함
적합: 정기 구독 리포트로 확장할 때
추천: 초기 MVP는 B로 시작해 진입장벽을 낮추고, 구독형 리포트를 낼 때 C를 더하는 순서를 추천해요. A는 사용량이 쌓인 뒤 파워유저용 고급 모드로 나중에 얹는 게 자연스러워요.
6. 컴포넌트
버튼
Primary: `background: var(--color-primary)`, 텍스트 흰색, `border-radius: var(--radius-md)`, hover 시 `--color-primary-hover`
Secondary: 흰 배경 + `1px solid var(--color-border)`, 텍스트는 `--color-text-primary`
한 화면에 Primary 버튼은 하나만 — 나머지는 Secondary
카드
`background: var(--color-surface)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-lg)`, `padding: 20px`
입력창
높이 44px, `border-radius: var(--radius-sm)`, focus 시 테두리 `--color-primary`로 전환
placeholder는 실제 입력 예시로: "예: 크리스마스 케이크 주문" (❌ "키워드를 입력하세요")
배지/태그
배경은 해당 색상의 옅은 톤, 텍스트는 진한 톤 (예: 상승 추세 배지 → 옅은 초록 배경 + 진한 초록 텍스트)
7. 보이스 & 톤
쉬운 말 우선: "CTR" → "클릭률", "트래픽" → "방문자 수"
문장은 짧게, 존댓말(해요체)로 통일
에러 메시지는 원인 + 다음 행동을 함께: "이 키워드는 검색량이 너무 적어요. 다른 표현으로 다시 검색해보세요."
빈 상태는 위축시키지 않고 다음 행동을 안내: "아직 등록한 키워드가 없어요 → 첫 키워드 등록하기"
숫자는 항상 해석을 곁들인다: "1,200회 (같은 업종 평균보다 높아요)"

8. 레이아웃 & 인터랙션 — Apple 스타일 (2026-07 추가)
색상(코랄/앰버)과 폰트(Pretendard)는 그대로 유지한다. 아래는 여백·타이포 스케일·움직임에 대한 결 방향으로, "Apple스럽다"는 브랜드를 흰색/회색/파란색으로 바꾸는 게 아니라 여백을 넉넉히 쓰고, 헤드라인을 크게, 움직임을 부드럽게 가져가는 것을 뜻한다.

원칙
- 한 화면 = 한 가지 메시지 (기존 원칙의 강화판): 섹션마다 핵심 메시지 하나, 다음 메시지는 스크롤해야 나오게
- 여백이 곧 위계: 테두리·구분선 대신 여백 차이로 섹션을 구분하는 걸 기본으로 하고, 테두리는 카드처럼 꼭 필요한 곳에만
- 정적인 것보다 반응하는 것: 스크롤/탭/호버에 뭔가 살짝 움직여야 살아있게 느껴진다 (단, 존재감이 과하면 안 됨 — "살짝")

여백 스케일
- 섹션 세로 패딩: 데스크톱 96–140px(`py-24`~`py-32`), 모바일 56–80px(`py-14`~`py-20`) — 히어로 섹션이 가장 크고, 아래로 갈수록 조금씩 줄어도 됨
- 카드/패널 내부 패딩: 데스크톱 20–24px, 모바일 16px (이미 적용된 `p-4 sm:p-5` 패턴 유지)

타이포 스케일 확장
- 기존 H1(28px)보다 큰 "Display" 크기를 히어로 타이틀 전용으로 추가: 모바일 32–36px → 데스크톱 56–64px, weight 800, letter-spacing -0.02em
- 굵은 헤드라인일수록 letter-spacing을 살짝 좁힌다(-0.01~-0.02em); 본문/캡션은 기존 값 유지

컴포넌트 확장 토큰 (기존 토큰은 안 건드리고 추가만)
```css
--radius-xl: 24px;          /* 히어로/기능 카드처럼 큰 카드용, radius-lg(16px)보다 한 단계 큼 */
--ease-spring: cubic-bezier(0.22, 1, 0.36, 1);  /* 스크롤 진입, 버튼 프레스 등 모든 인터랙션 이징에 공통 사용 */
```

인터랙션
- 버튼 프레스: 클릭/탭 시 `scale(0.97)`로 살짝 눌렸다가 `--ease-spring`으로 되돌아옴 (이미 주요 버튼에 적용됨)
- 스크롤 진입 애니메이션: 섹션이 뷰포트에 들어올 때 `opacity 0→1` + `translateY 16px→0`, 400–600ms, `--ease-spring`. IntersectionObserver 기반, 한 번 재생되면 다시 숨기지 않음(스크롤을 위아래로 반복해도 깜빡이지 않게)
- 페이지 전환: 내비게이션마다 새 페이지가 살짝 fade+slide-up로 들어옴 (`template.tsx`에 이미 구현됨) — Apple 스타일에서는 이 duration을 300ms 내외로, `--ease-spring` 사용
- `prefers-reduced-motion: reduce`인 사용자에게는 스크롤 애니메이션과 프레스 스케일을 전부 생략하고 즉시 표시 — 접근성 예외 없음

## Forbidden
- 디자인 시스템을 벗어난 컴포넌트 생성 금지
- 중복 컴포넌트 생성 금지
- 디자인 토큰 임의 변경 금지 (섹션 8의 신규 토큰처럼 **추가**는 허용, 기존 값 변경은 금지)